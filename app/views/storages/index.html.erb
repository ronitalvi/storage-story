<div class="card-c">
  <div class="cardsplate">
  <% @storages.each do |storage| %>

  <div class="col-xs-12 col-ms-6 col-md-6 ">
  <div class='card' data-id='<%= storage.id %>' style='background-image: url(<%= storage.photo %>);'>
    <div class='card-details-wrapper card-sqm-wrapper'>
      <div class='card-details-text'><%= storage.sqm %> m<sup>2</sup>
      </div>
    </div>

    <div class='card-details-wrapper card-price-wrapper'>
      <div class='card-details-text'>₪ <%= storage.price %>
      </div>
    </div>

    <div class='card-title-wrapper'>
      <div class='card-title-text'><%= storage.name %>
      </div>
    </div>
  </div>
  </div>
  <% end -%>
  </div>
  <div class="col-xs-12 col-ms-6 col-md-6 ">
<div
  id="map"
  data-markers="<%= @markers.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
></div>
</div>
</div>

