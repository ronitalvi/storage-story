<p id="notice"><%= notice %></p>
<h1>Flats</h1>
<div class="container">
  <div class="row">
    <div class="col-sm-4 col-sm-offset-2">
      <table>
        <thead>
          <tr>
            <th>Adress</th>
            <th>User</th>
            <th colspan="3"></th>
          </tr>
        </thead>
        <tbody>
          <% @flats.each do |flat| %>
          <tr>
            <td><%= flat.address %></td>
            <td><%= flat.user[:email] %></td>
            <% if policy(flat).show? %>
            <td><%= link_to 'Show', flat %></td>
            <% end %>
            <% if policy(flat).edit? %>
            <td><%= link_to 'Edit', edit_flat_path(flat) %></td>
            <% end %>
            <% if policy(flat).destroy? %>
            <td><%= link_to 'Destroy', flat_path(flat), method: :delete, data: { confirm: 'Are you sure?' } %></td>
               <% end %>

          </tr>
          <% end %>
        </tbody>
      </table>

      <br>
      <% if policy(Flat).new? %>
      <%= link_to 'New Flat', new_flat_path %>
      <% end %>
    </div>
  </div>
</div>
<div
  id="map"
  style="width: 100%;
  height: 100vh;width: 500px; position: fixed;top: 0;right: 0;"
  data-markers="<%= @markers.to_json %>"
  data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>"
></div>
